spring:
  application:
    name: payment-gateway

server:
  port: 8090

springdoc:
  swagger-ui:
    enabled: true
  api-docs:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,metrics

bank-simulator:
  base-url: http://localhost:8080
